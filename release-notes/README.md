# Release notes

Keep one concise English note for the current private-copy release. Historical multilingual upstream notes are intentionally omitted.

The release workflow validates the note matching the pushed version tag and publishes only the extension ZIP and its SHA-256 checksum.
